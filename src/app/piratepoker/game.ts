import { Player } from './player'
import { Deck } from './deck';

export class Game {
    private players: Player[] = [];
    private deck: Deck;
    private roundNumber: number;
    private maxScore: number;

    private cpu1: Player;
    private cpu2: Player;
    private cpu3: Player;
    private cpu4: Player;
    private cpu5: Player;
    private cpu6: Player;
    private cpu7: Player;
    private cpu8: Player;
    private cpu9: Player;
    private cpu0: Player;

    public dealCards(arrayOfPlayers){

    }







    }
